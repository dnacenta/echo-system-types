[package]
name = "echo-system-types"
version = "0.3.0"
edition = "2021"
description = "Shared contract types for the echo-system plugin ecosystem"
license = "AGPL-3.0"
repository = "https://github.com/dnacenta/echo-system-types"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
